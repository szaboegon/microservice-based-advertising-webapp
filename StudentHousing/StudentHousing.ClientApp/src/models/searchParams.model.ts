export interface SearchParams{
    categoryName: string,
    city: string,
    numberOfRooms: number,
    minSize: number,
    maxSize: number,
    minMonthlyPrice: number,
    maxMonthlyPrice: number, 
    furnished: boolean,
    parking: boolean,
}